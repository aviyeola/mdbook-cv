[package]
name = "mdbook-cv"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.2", features = ["derive"] }
anyhow = "1.0"
walkdir = "2.3"
reqwest = { version = "0.11", features = ["blocking", "rustls-tls"] }

[profile.release]
opt-level = 3
